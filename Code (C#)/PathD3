using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PathD3 : MonoBehaviour {

	// Use this for initialization
	void Start () {
        // iTween.MoveTo(gameObject, iTween.Hash("path", iTweenPath.GetPath("New Path 4"), "movetopath", false, "time", 8, "easetype", iTween.EaseType.easeInOutSine, "looptype", "loop"));
        iTween.MoveTo(gameObject, iTween.Hash("path", iTweenPath.GetPath("New Path 4"), "movetopath", false, "time", 8, "lookahead", 0.001f, "easetype", iTween.EaseType.easeInOutSine, "islocal", true, "looptype", "loop"));
    
}

    // Update is called once per frame
    void Update () {
		
	}
}
